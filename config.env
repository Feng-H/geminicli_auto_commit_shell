# 自动提交功能的配置文件
# Configuration for Auto-Commit

# 检查间隔（仅用于监控模式下的轮询回退）
CHECK_INTERVAL=10

# Diff 分析的最大行数（避免 Token 超限）
MAX_DIFF_LINES=200

# Gemini 提示词模板
# 注意：脚本中会使用 eval 或简单的变量替换，尽量保持简单
PROMPT_TEMPLATE="You are an automated git commit message generator. Analyze the following git diff and generate a single, concise commit message adhering to Conventional Commits. Return ONLY the raw commit message string. Diff Content:"
